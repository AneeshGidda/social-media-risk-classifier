from pathlib import Path

ROOT_DIR = Path(__file__).resolve().parent
RAW_DATA_PATH = ROOT_DIR / "data" / "dataset.csv"
FEATURES_DIR = ROOT_DIR / "models" / "features"
ARTIFACTS_DIR = ROOT_DIR / "models" / "artifacts"
